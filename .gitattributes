# Text files are edited in the working tree as CRLF.
* text=auto eol=crlf

# Binary assets must not be line-ending normalized.
*.cache -text -eol
*.db -text -eol
*.dll -text -eol
*.ogg -text -eol
*.pdb -text -eol
*.png -text -eol
